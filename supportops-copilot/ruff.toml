line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
# B008: `Depends(...)` in a parameter default is the FastAPI idiom, not a bug.
ignore = ["B008"]

[lint.per-file-ignores]
"tests/*" = ["E501"]
